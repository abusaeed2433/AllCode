#include<stdio.h>
#include<string.h>
int main()
{
    char ch[100],a,b,ch1;
    int i,j,c,x,n;
    scanf("%d",&n);
    for(i=1;i<=n;i++)
    {
        scanf("%s",ch);
        for(i=1;i<=100;j++)
        {
            ch1[100]=strcpy(ch);
        }
    }
    label:
    x=strlen(ch);
    for(i=0;i<100;i++)
    {
        a=ch[0];
        b=ch[x-1];
        c=x-2;
    }
}

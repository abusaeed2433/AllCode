#include<stdio.h>
int main()
{
    int t,i,j,k,n;
    scanf("%d",&t);
    for(i=0;i<t;i++)
    {
        scanf("%d",&n);
        int a[n],sum=0;
        for(j=0;j<n;j++)
        {
            scanf("%d",&a[j]);
        }
        if(a[0]>0)
        {
            y=a[0];
            for(j=1;j<n;j++)
            {
                if(a[j]>0)
                {
                    if(a[j]>y)
                    {
                        y=a[j];
                    }
                }
                else
                {
                    s
                }
            }
        }
        else
        {
            a
        }
    }
}
